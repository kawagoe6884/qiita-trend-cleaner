# Qiita Trend Guard — プロジェクト規約

## ⭐ 最終目的を見失わないこと

このプロジェクトには**目的が 2 つ**ある。拡張機能を作ることだけが目的ではない。

1. **Qiita のトレンドを健全化する Chrome 拡張を完成させる**
2. **⭐ その過程すべてを Qiita 記事として投稿する**（PRD の Phase 10）

**2 番目を忘れないこと。** 「PRD を書き、調査で前提が覆り、設計を作り直し、実装し、実機で壊れて直した」という**過程そのものが記事の主題**であり、拡張機能はその題材である。記事の主題は **拡張機能の紹介 と 開発プロセス の両方**（ユーザー確定済み）。

### 記事の素材として既に確保できているもの

| 素材 | 内容 |
|---|---|
| 設計の転回 | スクレイピング前提で設計 → 公式ヘルプの禁止規定を発見 → Atom フィード + API で再設計 |
| 見つけにくい一次情報 | `help.qiita.com/ja/articles/qiita-api` は API ドキュメントからリンクされておらず気づきにくい |
| 直感が外れる例 | Atom フィードの `<updated>` に数分のラグがあり、時刻トリガーでは古いセットを掴む → 変化検知に切り替え |
| 誰でも踏む罠 | DevTools でコピーした CSS-in-JS のハッシュクラス名（`.style-*`）は次のデプロイで壊れる。ARIA 属性とテキストが実質的な公開 API |
| 実装で実際に壊れた話 | エントリを両方 `index.ts` にしたら @crxjs のチャンク名が衝突し、service worker が一度も実行されなかった |
| 検証の穴 | ビルド成功・型 OK・lint OK・テスト全通過でも成果物の配線は壊れうる。実機確認で初めて露見した |
| マスクが検証を殺した | 実測した URL を報告時に丸ごとマスクした結果、UTM パラメータの存在が視界から消え、パーサが実データで全件落ちた。テストは誤った理解のままの合成データで通り続けた |
| CSS が hidden を殺した | `display: flex` を足した瞬間、UA スタイルの `[hidden]{display:none}` が負けて要素が消えなくなった。型・テスト・ビルドすべて通る。jsdom は CSS のカスケードを評価しないため、実機でしか出ない |
| warn も「エラー」だった | 想定内の失敗を error から warn に下げても、Chrome の拡張機能ページは同じ欄に集める。ユーザーの入力ミスが「拡張の不具合」として記録され続けていた |
| 直したはずが経路を変えて生きていた | 401 を `qiita-client` で debug に下げた。だが `scanner` の catch が warn のままで、スキャン中の 401 は 1 回につき最大 30 行エラー欄に積まれ続けていた。**修正はレイヤーを 1 枚直しただけでは終わらない** |
| レート枠が「設計どおりの不具合」を生む | 30 分間隔 × 約 30 req = 60 req/h で、無料枠 60 req/h とちょうど同値。起動時スキャンが 1 回入るだけで 429 に届く。**正常な無料プランの挙動がエラーとして記録される。**定期実行を廃止して解決し、副産物として `alarms` 権限も落とせた |
| テストがバグを捕まえなかった | 「alarms を登録しない」テストは、起動しただけでは素通りした。`alarms.create` は `onInstalled` の中に書かれるため、リスナーを発火させないと観測できない。**テストを書いたら、直した箇所を戻して落ちることを必ず確認する** |
| モジュール実体が二重化する | `vi.resetModules()` 後の service worker は別インスタンスの logger を掴む。ファイル冒頭で import した logger に spy を張っても空振りする（stderr には出ているのに検出できない） |
| **公式が出す 2 つの入口が別物だった** | `popular-items/feed` と `/trend` の一致率は **70%**。共通 21 件・各 9 件が相違。**フィードを信じて作った Phase 4 は、ユーザーが見ている 9 件を永久にスキャンできない設計だった。**実装が全部通り、実機でも動き、5 件検出できてもなお、見ている対象が違っていた |
| 「原理的に不可能」と書いた予測が外れた | 「トレンド 30 件に同一著者が 2 本入るのは稀」と計画に断言したが、実測では 5 著者だけで 30 枠中 13 を占めていた。**著者の重複は稀どころか常態。**推測を計画に断定形で書くと、後続の設計判断がそこから出発してしまう |
| 禁止の回避ではなく前提の消去 | スクレイピング禁止に対し、`robots.txt` が `/trend` を開放していることを根拠にする道もあった。だが**表示中の DOM を読めばリクエストが 1 本も増えず、規約の解釈そのものが不要になる。**制約は迂回するより消す方が強い |
| 敵対的な相手を想定すると設計が変わる | 「likes を再取得するか」は枠の節約だけなら 1 度でよい。だが**拡張の存在が知られたらいいねの時刻をずらされる**。初回スナップショットしか持たないと、あとから揃えられた分を永久に見落とす |
| 予測をやめると設計が減った | レート枠を予測する方式を捨てただけで `RATE_SAFETY_MARGIN` / `availableRequests` / `fallbackLimitFor` / `ScanProgress.budget` / `truncated` の伝播という **5 つの概念が消えた**。増えたのは例外型 1 つ。**不確実性を予測で埋めると、予測の精度という別の不確実性が生まれる** |
| テストが落ちない理由を確かめる | 実装を 14 通り壊して 13 通りは捕まえた。捕まらなかった 1 件は**バグではなくコメントの誤り**を示していた（重複排除の本体は Map のキーで、消した行は最適化にすぎなかった）。「テストが落ちないから消してよい」ではなく、**落ちない理由を確かめる**ところまでやらないと判断できない |
| もっともらしい失敗ほど疑う | 変異テストのハーネスが 13 件すべて「捕まらない」と報告した。壊れていたのは**ハーネス自身**（vitest 4 に無い `--reporter=basic` を渡していた）。**全件が同じ結論に揃うこと自体が異常の兆候** |
| 最大リスクが空振りした | 「`document_idle` で遅延描画されていたら 0 件」を最大リスク（中／高）と書き、**MutationObserver を先回りで入れなかった**。実機では 1 発で揃っていた。**入れていたら、必要だったのかどうかが永久に分からなくなっていた** |
| 入力がユーザーの状態に依存し始めた | Atom フィードは誰が見ても 30 件だが、**トレンドページはミュート済みの分が消えて 25 件**。DOM に切り替えたことで、拡張の入力がユーザー自身の操作履歴に依存するようになった。枠の節約という良い副作用がある一方、**誤検知でミュートしたアカウントは視界から消え、再評価できなくなる**（適合率フィードバックはミュート前に取るしかない） |
| 同じ語を 2 つの意味で使ったログ | `trend items: 25`（入力件数）の次に `scan finished: items: 0`（取得件数）を出していた。全件既知のときの `items: 0` が「何も取れなかった」と読める。**実機ログを人間が読んで初めて気づく類の欠陥で、テストは通る** |
| 契機を変えると、変えていないコードの前提が壊れる | スキャンの起動を「ページを開いたとき」に変えた瞬間、**2 タブ開くだけで runScan が重なる**ようになった。開始時に読んだインデックスを数秒持ち回って最後に保存する形は、それまで安全だったのに lost update になった。**触っていない行が壊れる** |
| エラーが出ないことと、やるべきでないことをやらないことは別物 | プロフィールページにも記事リンクと `<time>` が揃っているため、**トレンド以外のページでもスキャンが走っていた**。エラーは 1 行も出ない。実機チェックリストに「記事ページを開いても何も出ない」と書いておきながら実施せず、レビューで初めて出た |
| 実機確認は「試した経路」しか保証しない | 「リロードでは API を叩かない」をライトモードの実機ログで確認して合格にしたが、**フルモードでは著者一覧を毎回叩いていた**（`fetchUserItems` が `seen` を見る前に呼ばれる）。ユーザーはトークン未設定なので、**この経路のログは永久に出ない**。検証ループの差分レビューで初めて見つかった |
| 寿命の違うデータを同じ構造に入れない | 「候補」は閾値を変えるたび再計算で作り直されるが、「評価」は人間が積み上げた資産で再計算では戻らない。`Candidate.verdict` のままだと**スライダーを 1 つ動かした瞬間に指標そのものが消えていた** |
| 分母 0 を 0% にするか「—」にするかで判断が変わる | 「測ったら 0% だった」と「まだ測っていない」は別物。取り違えると Phase 9 の閾値調整の方向が逆になる |
| UI の手抜きが指標の妥当性を殺す | 根拠記事へのリンクが無ければユーザーは記事を読めず、「妥当 / 誤り」は当てずっぽうになる。そこから出る適合率は無価値。**リンクは飾りではなく指標の一部** |
| UI の不具合を「見た目の問題」と思い込むと計算量を数えない | つまみが動かない原因を 2 度レイアウトのせいにして 2 度外した。真因は `input` 1 回あたり storage 全体を 2 回読んで 300 アカウント分の検出を回していたこと。**`input` は 1 px ごとに発火する** |
| 同じ処理に入口が 2 つあると片方だけ直して直ったつもりになる | マウス（`input`）の storm は直したが、キーボード（`change` のリピート、毎秒数十回）は残っていた。**Phase 4b の「ログ水準の修正は経路ごとに漏れる」と同じ形が、性能の層で再発した** |
| デバウンスは頻度を下げるが同時実行は止めない | タイマーは発火した瞬間に手を離すので、実行中の処理は次を止めない。**頻度の制御と排他は別の問題** |
| ゲートに無い工程は実行されない | `npm run lint` は ESLint だけで整形を見ない。`npm run format` はどのゲートにも入っておらず、**10 ファイルが静かにずれていた**（4 つは Phase 4b から） |
| 落ちようがないテストは消すか「守られていない」と書く | 変異を入れても落ちないテストを 2 度書いた。残すと、次に触る人が守られていると思って壊す |
| 受け入れ基準を絶対形で書くと正しい実装が基準に反する | 「`logger.error` を増やさない」と書いたが、本来の規約は「**想定内の失敗を**載せない」だった。基準は禁止したい失敗の形で書く |
| 例外型の置き場所はテスト戦略に依存する | `RateLimitError` を投げる側のモジュールに置くと、そのモジュールを `vi.mock` するテストで `instanceof` が**実行時に**壊れる。型エラーは出ない。例外型は `lib/errors.ts` に集める |
| CSS で直せない見た目がある | ポップアップの窓幅が候補の有無で 15px ずれた。`scrollbar-gutter: stable` でも `overflow-y: scroll` でも直らない。**Chrome は窓幅を body 幅で決め、中身が 600px を超えたときだけスクロールバーぶんを外側に足す** — どちらの CSS も窓の寸法決めに関与しない。**2 度同じ方向で外し、実測を取るまで仮説を切り替えられなかった** |
| 測る道具の摩擦が測定値を歪める | 根拠リンクを同じタブで開くとポップアップが閉じ、1 件評価するのに開き直しを含めて 2 往復かかっていた。**適合率を測るための UI なのに、測る操作が一番重い。**面倒な計測は実行されず、実行されない計測の値は存在しない |
| 「受け入れたトレードオフ」と書くと検討が止まる | stale な状態を「ポップアップは短命だから実害は小さい」と記録した。だが見ていたのは**無害な方向（バッジの `!` が残る）だけ**で、有害な方向（**`!` を消して枠切れに気づけなくする**）は書いていない。**制約を認めて記録する行為そのものが、打ち切りの口実になる** |
| 人間の目視が指標より先に真実に届いた | ユーザーが「同じ組織・共通のいいね」で見つけた 2 著者を、拡張は 1 件も検出できなかった。しかも `emptyAccountRatio` は共通グループを **6%**（全体 38%）と、**より健全に見せていた**。**指標を作った側は、指標の外を見なくなる** |
| 取得の射程と判定の窓を別々に決めると、取ったそばから捨てる | フルモードは過去記事を取りに行くのに、`withinLookback` は `itemPostedAt` で 3 日で切る。**89 件のいいねを取得して判定に 0 件使用。**ログは `fetched: 4 likes: 89` と成功しか出ず、`detected 0` は「該当なし」と区別がつかない。**エラーは 1 行も出ない** |
| バグを直したら反対側の端に振り切れた | 「リロードで無駄に 30 req 使う」を直した結果、**トークンを設定しても 1 req も使わなく**なった（著者を `newItems` からしか取らない）。同じ関数で前回は叩きすぎ、今回は叩かなすぎ。**修正の方向を決めるとき、反対側の端を見ていなかった** |
| 閾値は調整できるが、判定単位は調整できない | 「同じ著者の M 本に同じ顔ぶれ」と決めた瞬間、「**1 人 1 本ずつ投稿して互いに押し合う**」形が原理的に見えなくなっていた。記事 1 本の著者はスライダーをどう動かしても出ない |
| 異常値は母集団を数えて初めて異常だと分かる | 「共通 17 人」だけでは判断できない。27 記事・351 ペアを全部数えて「**5 人以上重なるのは 1 組だけ**」（2 位は 3）と分かって初めて、閾値を置ける場所が見えた |
| 同じ形の窓が 3 つあり、1 つ直しても残りが同じことをしていた | 判定の窓（`withinLookback`）を 7 日にしたら候補が出た。だが**保存の窓（`purgeLikeIndex`）が同じ `itemPostedAt` で切っており**、462 件取得して 427 件（61%）が保存前に消えていた。取得の射程は件数（2 本）だけで時刻を見ない。**3 つが別々の Phase で別々に決まっていた** |
| 弱いテストは、変異を入れるまで弱いと分からない | 「429 で訪問記録を書かない」テストは、**2 人目の著者が未訪問のまま残る**せいで変異を入れても通っていた。アサーションが真になる経路が 2 つあると、狙った経路を検査できていない。429 は普段起きないので**実機でも永久に見えない** |
| 状態を持たずに済ませようとして、持つべき状態を推測していた | 「その著者を訪れたか」を記事 ID の集合から判定しようとした。**記事が 1 本しか無い著者と、まだ辿っていない著者が同じ見え方になる。**訪問記録という状態を 1 つ持つだけで消える問題だった |
| 「実測ではこうだった」は正しさの証明にならない | 著者間クラスタで **1 つの組織しか観測していない**ことを根拠に、独立した 2 組織が混ざる欠陥を「記録だけ」して放置した。レビューが実際にコードを走らせ、**UI が「同じ顔ぶれが〈重なりゼロの著者〉の記事にも現れています」と嘘をつく**ことを示した。**観測の範囲を実装の保証にすり替えていた** |
| 同じミスは 3 回目まで気づかない | JSDoc と関数のあいだに新しい関数を挿入して孤立させる事故を 3 回やった。3 回目は `git diff` を目で追って自分で見つけた。**差分を読む工程を挟むまで、レビュー任せだった** |
| DevTools からコピーした HTML には自分の環境が混ざる | トレンドカードの実測に `fdprocessedid` があった。**Qiita のものではなく、パスワードマネージャー系の拡張が付けた属性。**他人の環境には存在しない。CSS-in-JS のハッシュ名と同じ罠が、**自分が入れている拡張**という別経路から来る |
| 危険な選択肢が安全な選択肢の真上にある | 三点メニューは「フォロー / **ブロック** / ミュート」の順。1 つ上を押すとブロックで、**native `alert()` が出て閉じられず、解除一覧も無いので誤検知を回収できない**。順序やインデックスで選ばず、テキスト完全一致でのみ特定する |
| 読めるようになった情報を、使ってよい情報と混同しない | トレンドカードから**組織名が追加リクエストゼロで読める**と分かった。だが組織で判定すると**特定企業を狙い撃ちする機能**になる。同じ会社の人が Qiita に書くのは正常であり、**取得可能性は使用許可ではない** |
| 文字列がシェルを 1 枚通るたびに削れる | ドキュメントに書いた識別子（バッククォート囲み）が、`node -e` に渡す途中でシェルに解釈されて**丸ごと消えた**。「〜。 は変更しない」という壊れた文が残る。正規表現のバックスラッシュでも同じことを 2 度やった。**通す層が増えるほど、エスケープの規則が重なる。**長い文字列はファイルに書いて経由させる |
| storage の変更通知は「操作の通知」ではない | 「表示する」で戻したあと「妥当」を押しても何も起きなかった。**評価が既に valid なので値が変わらず、`chrome.storage.onChanged` が発火しない。**ユーザーの操作を storage の変化で代用すると、**冪等な操作が消える** |
| 一方向のボタンは、押した後の状態を放置する | 「表示する」を作ったとき、押した後にどうなるかを考えていなかった。実機チェックリストに「表示する を押す → 戻る」はあったが、**その次の行が無かった**。手順を 1 つずつ確かめても、**手順の間の状態は検査されない** |
| **教訓を書いても手順を変えないと 3 回踏む** | 「長い文字列はファイルに書いて経由させる」と記録した **30 分後**に再発（2 回目）。さらに検証ループ中、Markdown の表を `node -e` に直接渡して**バッククォート識別子 6 箇所が空セルになった**（3 回目）。**書くだけでは行動が変わらない。**唯一効くのは手順を消すこと — **Markdown / 日本語の長文はシェルを通さず Edit ツールで直接書く**（`node -e` も heredoc も使わない） |
| ドキュメントの誤りはテストに映らない | 検証ループの差分レビューで、既定値を 60 → 180 に変えたのにコメント・テスト名・describe の説明が 60 のまま残っているのを 5 箇所見つけた。**アサーションは定数を参照しているので全部通る。**「ちょうど 60 分は窓内に入れる」というテスト名が嘘になっていた。**名前に値を書くと、値を変えたとき名前だけが嘘になる** |
| JSDoc の孤立が 4 回目。自分では見つけられなかった | 関数と JSDoc のあいだに新しい宣言を挿入する事故を 4 度やった。3 回目は `git diff` を読んで自力で見つけたが、**4 回目はツールの差分通知で気づいた**。「差分を読む」を手順に入れたつもりが、**書いた直後には読んでいなかった** |
| 見た目にも約束は及ぶ | 「不正と断定しない」（約束 6）を文言だけの規則だと思っていた。**背景色に赤を使えば、文言が中立でも断定している。**半透明の琥珀に留め、テストで赤系を弾くようにした |
| **教訓が初めて先回りで効いた** | 「storage の変更通知は操作の通知ではない」は Phase 7 の実機バグから得た教訓。Phase 8 では**設計の段階でそれを適用**し、`onChanged` にミュートをぶら下げる案を書く前に捨てた。既に「妥当」を押してある 2 件が永久にミュートされない不具合を、**実機に出す前に消した**。変異テストがその番人になっている |
| 等価変異が「測っていないこと」を教えた | 「解除の文言では落ちない」変異が捕まらず、**自分が置いたフィクスチャが推測でしかない**と露見した（解除の文言は意図的に測っていない）。推測に依存しない形（`${MENU_TEXT.mute}を解除`）で性質を固定し直した。**測っていないものをテストの前提に使っていた** |
| 同期処理は直列化のテストを無効にする | `muteAuthor` は `click()` まで全部同期で、テストの Snackbar も同期に出していた。**列を外しても順序が同じ**になり、直列化を検査できていなかった。非同期の境界を作らないと観測できない |
| 即座に解決する await は待っていない | `vi.waitFor(モックが呼ばれた)` は、そのモックが同期的に解決するなら**何も待たない**。後続の await 連鎖が始まる前にアサートしてしまい、2 件のテストが変異で落ちなかった。待つべきは**処理の末尾で起きること**（今回は再描画） |
| 知らない方が安全な情報がある | ミュート解除の文言を調べれば「既にミュート済み」を UI で判別できる。**だが調べない** — 実装がその文字列を知った瞬間、それを押す経路が生まれる。「一致しなければ何もしない」に閉じる方が強い |
| **フィクスチャが実機と違うと、テストは何も守らない** | Phase 8 は 17 変異すべてを捕捉して「守られている」と判断したのに、**実機では 1 件もミュートできなかった**。原因は 2 つとも**フィクスチャが実機と別物だったこと** — ①メニューを最初から DOM に置いていたので「押した瞬間はまだ無い」経路が一度も走らなかった（React は同期で描画しない。実測 click 直後 0 件 → 300ms 後 1 件）②項目のテキストをラベルだけにしていたので、**アイコンのリガチャが混ざる**（`volume_off投稿ユーザーをミュート`）ことに気づけなかった。**変異テストの網羅率は、フィクスチャの正しさを 1 ミリも保証しない** |
| **成功が次の失敗を作る** | ミュートは通った。だが**ミュートすると Qiita がその場でトレンドから記事を外す**ので、同じ候補で押し直すと `not-on-page` になり、UI が「**次に出てきたときに押し直してください**」と案内した。**ミュート済みの著者はもう出てこない。**起こり得ないことを促していた。`outcome`（最後の試行）だけでは足りず、**「成功したことがある」という消えない事実**（`mutedAt`）が要る |
| **見つけることと、押せることは別** | ①描画待ちと②アイコンのリガチャを直したら、今度は `timeout`（押したのに何も起きない）になった。**`[role="menuitem"]` は器で、ハンドラは中の `<button>` にある。**イベントは下へ伝播しないので器を押しても無反応。**3 回とも「実測 → 直す」を繰り返して初めて動いた。**1 回の実測で全部見えると思ってはいけない — 手前の欠陥が奥の欠陥を隠す |
| 一致規則は「どちら側に何が付くか」で決める | アイコンは**必ず前**に付く。意味を変える語（「〜を解除」）は**必ず後ろ**に付く。だから `endsWith` がちょうど手口に合う。**`includes` は後置きを許してしまい、解除を押す。**完全一致はアイコンで落ちる。3 つのうち 1 つだけが正しく、それは**実測の形を見て初めて決まる** |
| 使わないと決めた情報が安全を作っていた | ユーザーページの項目は「**ミュートする**」で、トレンドページの「投稿ユーザーをミュート」と**一文字も重ならない**。使わないと決めた情報だが、**末尾一致である限り、誤ってユーザーページの DOM を掴んでも何も押さない**という保証をこの不一致が与えている |
| **下限フィルタは、変数を攻撃側が握っていると盲点になる** | `burstScore` の下限を作った。ユーザーが「**120 分後にイイネすれば回避できるよね？**」と指摘して初めて気づいた。**できる。しかも自分が書いたテストが、その回避を「正しい挙動」として固定していた**（90 分後の一斉いいねで候補ゼロ）。人数や記事数を減らすのは押し上げ効果を削るので攻撃側にコストがあるが、**時刻をずらすのはコストが小さい。**しかも取りこぼしたことがユーザーに見えない — `emptyAccountRatio` を却下した理由を、別の指標で作り直していた |
| 候補を減らす設定は、減った理由が見えない | 開放してよいのは①減らす側の変数を攻撃側が握っておらず、②減らしたことが画面で分かるものだけ。**「ユーザーに決めさせる」は、決めた結果が見えるときだけ正しい** |
| 制約を外すと、残ったものの役割が変わる | 下限を消したら、幅のスライダーは「絞り込み」から**「探索」**の道具になった。60 分で 0.00 の著者が 180 分で 1.00 になれば、**ユーザー自身が遅延に気づく。**下限はその発見を先回りして潰していた |
| コードに書いてあった規則を、実装時に自分で書き換えた | `burst.ts` のヘッダーには元から「**どちらも補強スコアであって判定条件ではない**」とあった。Phase 9 でそれを「判定条件になった」に書き換えたのが誤り。**過去の自分が置いた歯止めを外すときは、外す理由を歯止めより強く書けるか確かめる** |
| **罠を正しく特定してもテストが同じ罠を踏む** | 計画に「`asPositiveInt(0)` は既定が 0 だから偶然動く」と書き、対策の関数（`asNonNegativeInt`）も分けた。**なのにテストが同じ理由で無力だった** — 既定が 0 なので、どちらの実装でも答えが 0 で一致する。変異テストだけがそれを教えた。**罠を知っていることと、罠を検査できていることは別** |
| 「起きないこと」の検査は、起きうる時刻を過ぎてから見る | 「折りたたみを変えても再検出しない」を `selectFold()` の直後に同期でアサートしていた。**デバウンス 250ms のせいで、何を足しても通る。**時刻を進めて初めて変異を捕まえた |
| `textContent` は導線を殺す | 解除の案内（設定 > ミュート）を文字列で組み立てた瞬間、リンクがただの文字になった。**誤検知を回収する経路そのものが押せなくなる**のに、型もテストも通る。文言にリンクが要るなら HTML に静的に置く |
| 既定値を変えないことは、既存テストが無改変で通ることで証明できる | Phase 9 は設定を 2 つ増やしたが、`detector.test.ts` 22 件と `burst.test.ts` 15 件が 1 文字も変わらずに通った。**書き換えが必要になった時点で、それは仕様変更である** |
| 番人はファイル名から見つからない | `DEFAULT_SETTINGS` の中身を丸ごと固定する番人が `selectors.test.ts` に住んでいた。**全体テストを回すまで存在に気づかない** |
| ツールの拒否と再実行がコードを二重にする | 同一ファイルへ並列に編集を投げるとゲートが 1 本目を弾き、再実行で**二重適用**される。今回 2 回踏んで両方その場で直したが、**過去のセッションでは同じ事故が `popup-state.test.ts` の JSDoc 重複としてコミットまで通っていた**。同一ファイルの編集は直列にする |
| 誤読を防ぐ制約を「語の禁止」に翻訳すると正しい用法まで消える | 「分母が逆の主張をしない」ために、テストに `not.toContain('顔ぶれ')` と書いた。**禁じるべきだったのは述語側の形**（`〜が同じ顔ぶれ`）**だけ**で、主語側（`この顔ぶれのいいねの〜`）は正しい用法だった。結果、残った文が「いいねの 98%」＝全部のいいね、と読める形になり、**ユーザーが誤読して初めて分かった** |
| **割合は分母を隠す** | 「いいねの 100%」は 10/10 とも 5/5 とも読める。しかも**上の行の「5 アカウントが 2 記事に共通」を掛けても確定しない** — `qualifying` は N 人を超えうるし、全員が全記事に付けているとは限らない。**実件数にすると分母が 10 と 5 になり、「単位が違う」（いいね／アカウント）も同時に直る。**％を 2 つ並べた時点で、その区別は文言でしか支えられていなかった |
| **正しい疑問から逆の結論が出た** | 「`burstScore` は累積なのだから 1 日・2 日は要らない」は筋の通った推論だった。だが**トレンドの最古が 3 日前**（実測 2026-08-25）なので、+30h に揃えられた分は 720 分までだと 0 件のまま見えない。**目盛りの上限は、測る対象の寿命で決まる。**同じ実測が同時に、**上端（2 日）が観測範囲（3 日）より短い**ことも示した — **7 段は測らずに決めていた** |
| **「1 ページ目」が先頭とは限らない** | `fetchLikes` は `page=1` しか取らない。likes は**降順**で返るので、これは「**最も新しい 100 件**」だった。642 いいねの記事で **180 分以内のいいねが 0/100 件**。`burstScore` はそこにしか意味が無く、**クラスタ判定も遅れて押した層しか見ていない。**ページングを「取りこぼしの量」の問題だと思っていたが、実際は**取っている場所が逆**だった。エラーは 1 行も出ない |
| 捨てている値が答えを持っていた | 占有率を「今のデータでは計算できない」と**2 度断言した**。実際は `readArray` が全応答で `Total-Count` を読んでいて、`scanOneItem` が捨てていただけ。**追加リクエスト 0 で出せた。**手持ちを確かめずに「無い」と言っていた |
| 制約だと思ったものが自分の実装だった | 「likes は先頭 100 件しか取れない」と API の性質のように書いた。**`page` は普通に使える。**ユーザーが「1538 件の記事は `?page=77` まである」と指摘して崩れた。**自分のコードの限界を、外界の限界として報告していた** |
| ARIA 属性があっても、それが目的の値を指しているとは限らない | いいね数を読むのに `svg[aria-label="いいね"]` を起点にする案を出した。**label と数字を結ぶのは兄弟の並び順だけ**で、`aria-labelledby` も `role` も無い。順序が変われば**エラーにならずに違う数字を返す** — 約束 3 の「失敗したら null」が効かない壊れ方。「ARIA なら安全」ではなく、**その属性が値と結ばれているか**を見る |
| 読めるからといって読みに行かない | 上の案の見返りは「100 いいね超の記事 1 件につき 1 リクエスト」の節約だけで、その記事はほとんど無い。**同じ数字が `Total-Count` として毎リクエストで既に届いていた。**無契約のセレクタを 1 つ増やす前に、**手持ちで足りないかを確かめる** |
| **新しい前提に依存する機能は、既存データの上で死ぬ** | 窓内占有率の完全性判定を `Total-Count` の記録に依存させた。だがそれは**新しく取った記事にしか付かず**、`scanner` は**インデックス済みの記事を再取得しない**。**蓄積済みの候補は全部「測れません」**になり、ユーザーが実機で見て落胆した。テストは全部通っていた — **フィクスチャに新しい形しか置いていなかった**から。救う道は手持ちにあった: `per_page=100` で要求している以上、**保持件数が 100 未満なら「それが全部」と確定する**。新しいフィールドを足すときは、**それが無い既存データでどう見えるか**を先に確かめる |
| **テストがバグの側を固定していた** | `collectLikes` の `complete` は「次の周回で `page < 2` を見る」形で、**上限ちょうど（`lastPage = 5`）のときだけ立たない**。それを検査するはずのテストが総数 **450 = まさにその境界**を使い、「覆いきれなければ」という名前のまま**覆いきれてしまっていた**。しかもバグの値（`itemCoveredMinutes: 100`）を `toBe` で固定していたので、**直すとテストが落ちる。**捕まえ損ねたのではなく、**間違った側を守っていた。**見つけたのは外部のレビュー — **フィクスチャを置いた本人ではなかったから** |
| コメントの重複は 4 つのゲートを素通りする | 定数の出所を `rate-budget` へ移したとき、`PER_PAGE` の上に**古い JSDoc を残したまま新しいものを足し**、`/** */` が 2 枚重なった。**コミットまで通った。**JSDoc の孤立（記録 4 回）と同じ family だが、こちらは**行が増えるだけで何も壊れない**ため、型・lint・テスト・**自分の差分レビュー**が 4 つとも通る |
| レビューに前提を渡さないと、逆向きの指摘が返る | 汎用レビュアーに CLAUDE.md の約束（**想定内の失敗を warn / error に載せない**／レート枠を予測しない）を渡さずに回すと、「429 を warn にすべき」「残量から手前で止めるべき」が出る。**設計の意図は差分に書かれていない。**渡した上で出た 6 件は重複 1 組を含み、実質 5 件。**4 件が実在し、1 件は意図的な選択だった** |

### 記事化の絶対制約

**実アカウント名・具体的な記事 URL を一切掲載しない。** 誤検知による名誉毀損リスクと、利用規約第 11 条 5 項 1 号の回避のため。手法・統計値・開発プロセスだけで記事は成立する。一次証拠は PRD の Appendix に隔離してあり、**転載禁止**。

---

## 現在地

**Phase 2〜9 complete（4 を除く）。窓内占有率まで実機で動いている。次は「取得ページが逆だった」の修正（下記）／Phase 10（記事化）。**

- 設計: [PRD](.claude/PRPs/prds/qiita-trend-guard.prd.md) — **改訂 6**。意思決定ログと未解決事項はここ
- 完了した計画: `.claude/PRPs/plans/completed/`
- 実装レポート: `.claude/PRPs/reports/`

| Phase | 状態 |
|---|---|
| 2 基盤構築 / 3 トークン設定 UI / 5 検出エンジン | complete |
| 4 データ取得層（Atom フィード版） | **superseded** — 取得対象がユーザーの見ている画面と一致しなかった |
| **4b データ取得層（DOM 版）** | **complete**（実機確認済み） — [report](.claude/PRPs/reports/dom-trend-reader-report.md)。ただし **2026-08-25 に欠陥が 1 件見つかった**（下記） |
| **6 候補 UI** | **UI は動いた**（実機で候補・適合率・バッジを確認）。ただし**検出側の穴 3 件**が露見 — [report](.claude/PRPs/reports/candidate-popup-ui-report.md) |
| **5b-1 検出の穴を塞ぐ（バグ 2 件）** | **complete**（実機確認済み・**候補 1 件を検出**） — [report](.claude/PRPs/reports/detection-gap-fixes-report.md) / [調査](.claude/PRPs/reports/cross-author-collusion-investigation.md) |
| **5b-2 著者をまたぐ共起** | **complete**（実機で記事 1 本の著者を検出） — [report](.claude/PRPs/reports/cross-author-clusters-report.md) |
| **7 DOM 非表示** | **complete**（実機確認済み） — 評価済みを隠す / トグルで戻す / 妥当なカードに色 — [report](.claude/PRPs/reports/dom-hiding-report.md) |
| **8 ミュート実行（「妥当」と同時）** | **complete**（実機確認済み 2026-08-25） — [report](.claude/PRPs/reports/mute-on-valid-report.md)。既定オフのチェックで「妥当」と同時実行（一括は作らない）。**起動は message passing**（`storage.onChanged` では押し直しが効かない）。**ブロックがミュートの直上にあるので、末尾一致でのみ特定する**。**実測 → 修正を 3 周**して初めて動いた（描画待ち／アイコンのリガチャ／器ではなく中の button） |
| **9 判定基準をユーザーに開放する** | **complete**（実機確認済み 2026-08-25・**全 51 項目を 1 周で通過** — [report](.claude/PRPs/reports/user-configurable-thresholds-report.md)）。**開発者が閾値を追い込むのをやめた。**「投稿から何分以内」（60/120/180/360/720 分/1 日/2 日・**既定 180 分**）と折りたたみの対象（既定なし）をユーザーが決める。**設定は options ページに集約**し、折りたたみの 4 択には図解を添えた。**burstScore の下限は実装後に撤回**（120 分待てば回避できる＝変数を攻撃側が握っている）。空アカウント率・組織名も**開放しない** |
| 10 記事化 | pending |

**検出エンジン（`src/detect/`）は 4b の影響を受けない。** インデックスを受け取るだけで出所を問わない設計にしてあるため。

### 取得ページが逆だった（2026-08-25 実測 → **同日修正**）

**`fetchLikes` は `page=1` しか取っていなかった。likes は降順で返るので、これは「最も新しい 100 件」。**

642 いいねの記事（全 7 ページ）で実測:

| | `page=1`（いま取っている） | `page=7`（取れていない） |
|---|---|---|
| 投稿からの範囲 | 308.8h 〜 1192.8h | **0.1h 〜 19.3h** |
| 180 分以内のいいね | **0 / 100 件** | 5 / 42 件 |

**`総いいね − 100 ≧ 窓内のいいね` を満たすと、窓内が 1 件も取れなくなる。**`burstScore` だけでなく**クラスタ判定も遅れて押した層しか見ていない**（組織票は早く押す）。**エラーは 1 行も出ない。**

**直した**（`scanner.ts` の `collectLikes`）— 降順で**単調**、`Total-Count` が正確に返る、`page` で末尾を直接取れる、という 3 つが揃っていた。**100 件以下なら今までと同じ 1 req で完全。**100 件超だけ最終ページから遡り、**そのページで最も新しいいいねが最大の目盛り（2 日）を超えたら打ち切る**。上限 4 ページ。

**完全性の判定は「全部持っているか」ではない。**末尾から取ると「古い側は完全・真ん中が欠け・新しい側も完全」になるので、`LikeRecord.itemCoveredMinutes`（投稿から何分後までを全部持っているか）で `windowMinutes <= reach` を見る。**新しいフィールドが無い既存データは今までどおりの規則に落ちる。**

**深刻度は低かった**（2026-08-25 ユーザー実測）— トレンド 30 件のうち 100 いいね超は**ほとんど無い**。保険としての修正。**そのぶん実機で発火しにくく、テストでしか検証できない**（変異 10/10 捕捉）。詳細は [PRD](.claude/PRPs/prds/qiita-trend-guard.prd.md) の「取得ページが逆だった」。

**DOM は使わない。**トレンドカードに総いいね数は出ているが、`aria-label="いいね"` と数字を結ぶのが**兄弟の並び順だけ**で、順序が変わると**エラーにならずに違う数を返す**。`Total-Count` は既に毎リクエストで届いており、欠ければ `null` に倒せる。詳細は PRD の「トレンドカードのいいね数を読む案は却下した」。

### 窓内占有率（2026-08-25 実装済み・追加リクエスト 0）

`readArray` は全応答で `Total-Count` を読んでいるのに `scanOneItem` が捨てていた。いまは `LikeRecord.itemTotalLikes` に載せ、**分母が信用できるかの判定**に使う。

```
窓内占有率 = 窓内にいいねしたクラスタのアカウント数 / 窓内にいいねした実人数
```

**`burstScore` は画面から降ろした**（並び順のタイブレークには使う）。実測で 2 人の著者を区別できたのは占有率だけだった — どの幅でも 80〜95% の著者と、180 分で 79% ／ 2 日で 43% まで薄まる著者。

- **単位はアカウント。**(アカウント × 記事) の組で数えると 3 記事とも押した人が 3 回数えられ、見出しの「N アカウント」と食い違う
- **測れないときは `null`。**保持件数が `Total-Count` に満たない、または `Total-Count` 不明でちょうど 100 件持っている（切り詰めか偶然一致か区別できない）
- **空アカウント率は表示しない。**検出された 31 人の空率 61% に対し、同じ記事群の liker 全体が 54%、集団の外側だけでも 50% あった。**比較相手を決めない割合は強さを誤って伝える。**記録は続ける

**表示の語彙**: 「顔ぶれ」は使わず「**メンバー**」。Qiita 側の解除ページは「**ミュート設定**」。見出しに「共通」と書かない — `clusterSize` は連結成分の**和集合**であって、全記事に共通する集合ではない（26 いいねの記事に「30 アカウントが共通」と出て露見）。

### 実測で分かっていること（2026-08-23）— 検出に穴が 3 つ見つかった

ユーザーが目視で見つけた 2 著者（同一組織・いいねした顔ぶれが共通）を、**ライトでもフルでも 1 件も検出できなかった**。調査の全経過は [調査レポート](.claude/PRPs/reports/cross-author-collusion-investigation.md)。

| # | 欠陥 | 性質 |
|---|---|---|
| 1 | 著者巡回が `newItems` からしか著者を取らない | **リグレッション。**ライトで蓄積したあとトークンを設定しても、その著者の過去記事を永久に取りに行かない |
| 2 | `withinLookback` が `itemPostedAt` で切る | フルモードで取った過去記事が判定に入らない。**89 件取得して 0 件使用**（OQ-17） |
| 3 | 記事 1 本の著者は検出できない | 判定単位が「同じ著者の M 本」なので原理的に不可（OQ-18） |

**`emptyAccountRatio` はこの手口を素通りした。** 共通 17 人の空アカウント率は **6%** で、インデックス全体の **38%** より**低い**。OQ-15 は否定寄りで解決。

**異常値は母集団を見て初めて見える。** 27 記事・351 ペアのうち、いいね者が 5 人以上重なるのは **1 組だけ**（重なり 17／2 位は 3）。閾値を置ける場所はここで初めて分かった。

### 実測で分かっていること（2026-08-20）

- **初回スキャンで 5 候補**（蓄積ゼロ・ライトモード）。OQ-12 は肯定
- 空アカウント率が **`0.00 / 0.00 / 0.63 / 0.67 / 0.80`** と二極化。指標として機能している可能性が高い（OQ-15）
- ただし **`empty: 0.00` は「シロ」を意味しない** — 「大学の講義での相互 LGTM」なら学生は記事もプロフィールも持つ。空アカウント指標は手口の片方しか見ていない
- **適合率はまだ測れない。** 測る手段が Phase 6 のフィードバック UI であり、**測る前に閾値を動かすのは単一指標設計が避けようとしている失敗**

### 2026-08-19 の実測で前提が 2 つ覆った

- **likes API は認証不要。** 差はレート枠だけ（認証なし 60 req/h ／ 認証あり 1000 req/h）。トークンは必須ではなくなり、未設定でも動く **ライトモード**（トレンドの表示件数ぶんのみ・最大 30 req/スキャン。**ミュート済みの分は画面に出ないので入力から落ち、実機では 25 件だった**）を用意する。設定時は **フルモード**（著者の過去記事まで辿る・約 120 req/スキャン）
- **ミュートは likes API の結果に影響しない**（OQ-7 解決）。「ミュートするほど検出できなくなる」自己破壊的な挙動は起きず、実行前インデックスの保持は不要
- 副産物: likes の応答に **User オブジェクトが同梱**され、`Total-Count` / `Link` ヘッダーもある。空アカウント判定用の追加 API 呼び出しは不要

---

## 破ってはいけない設計上の約束

1. **スクレイピング禁止。** Qiita は公式ヘルプで明確に禁止している。データ取得は**公式 API と Atom フィードのみ**。HTML ページの自動 fetch は一切しない。表示中ページの DOM 読み取り（追加リクエストゼロ）は可
2. **DOM セレクタは `src/dom/selectors.ts` にのみ書く。** CSS-in-JS のハッシュクラス名（`.style-*`）は使用禁止。`src/dom/selectors.test.ts` が機械的に検査している
3. **DOM 取得の失敗は例外を投げず `null` を返す。** 誤った対象を操作するより何もしない方が無害。**この方針がチャンク名衝突バグの発見に直結した**
4. **`console` を直接呼ばない。** `src/lib/logger.ts` を通す（ESLint で強制）。**Chrome は `console.error` だけでなく `console.warn` も `chrome://extensions` のエラー欄に収集する**（2026-08-19 実測）。ユーザーの入力ミスのような想定内の失敗は `logger.debug` に留めること。warn / error は「拡張が壊れている」と読まれる
5. **エントリポイントに `index.ts` を使わない。** @crxjs がファイル名からチャンク名を導出するため、basename が衝突すると別々のローダーが同じチャンクを指す。`service-worker.ts` / `content-script.ts` のように一意にする
6. **判定 UI で「不正アカウント」と断定しない。**「不自然ないいねパターン」に留める
7. **ミュートがデフォルト。ブロックは一括自動実行しない。** ブロックは native `alert()` を起動し content script から閉じられない。かつ解除用の一覧 URL が存在せず誤検知を回収できない
8. **広告・収益化をしない。** Qiita ヘルプが利用規約違反と明記
9. **ライトモードとフルモードで判定ロジックを 2 本持たない。** `detectCandidates` は `ScanMode` を引数に取らない。違うのは**取得戦略**（どこまで辿るか・likes を再取得するか）だけで、判定は 1 本
12. **トレンドは Atom フィードではなく、表示中ページの DOM から読む。**（改訂 6）フィードとトレンドページは**別集合**で、実測の一致率は 70%。フィードを見ていると**ユーザーが画面で見ている 9 件を永久にスキャンできない**。`a[href*="/items/"]` を href で重複排除し（1 カードに 2 本ある）、`<time datetime>` から秒精度の投稿時刻（`2026-08-17T17:44:41Z`）を取る。**HTML の `fetch` はしない** — 表示中の DOM を読むだけならリクエストが 1 本も増えず、規約の解釈に依存しない
13. **レート枠を予測しない。429 が返るまで走り、返ったら止める。**（改訂 6）残量から手前で止める方式は、余白の見積もりという別の不確実性と、打ち切り状態の全レイヤー伝播を招く。インデックスは `itemId` で重複排除しながら蓄積するので、中断した残りは次にページを開けば自然に拾える。**429 は想定内の停止信号なので `logger.debug`**。ユーザーへはバッジとポップアップで「あと N 分」と伝える
10. **定期実行を持たない。`alarms` 権限を要求しない。** ライトモードは 60 req/h、1 スキャンは約 30 req。30 分間隔だと枠ちょうどで、起動時や手動が 1 回入るだけで 429 に届く。**「無料プランの設計どおりの挙動」がエラーとして記録されるのは設計の失敗。****起動契機はトレンドページを開いたときだけ**（改訂 6。`onInstalled` / `onStartup` の自動スキャンも廃止した。トレンドを取りに行かなくなった以上、ページを開いていない時点では見に行く先が無い）。`src/background/service-worker.test.ts` が番人として検査している
11. **想定内の失敗を warn / error に載せない。** 記事 1 件の欠損・著者 1 人の欠損・トークン失効は通常運転であり `logger.debug`。**全滅（30 件中 30 件が失敗）のときだけ warn を 1 行**。個々の失敗を warn にすると 1 スキャンで最大 30 行がエラー欄に積まれる。**ログ水準の修正は経路ごとに漏れる** — `qiita-client` で 401 を debug に下げても、`scanner` の catch が warn なら同じログが戻ってくる

---

## 環境の罠（毎回引っかかるので先に読むこと）

### Git Bash の `$PATH` が壊れている

Windows 形式（`;` 区切り）と POSIX 形式（`:` 区切り）が混在しており、`/usr/bin` が解決できない。`node` どころか `ls` すら見つからない。**すべての Bash 呼び出しの先頭に必要**:

```bash
export PATH="/usr/bin:/bin:/mingw64/bin:/c/Program Files/nodejs"
```

Node.js v24.13.1 / npm 11.8.0 は正常にインストールされている。

### Bash の heredoc は 140 行程度で切り捨てられる

`unexpected EOF while looking for matching '` で失敗する。**100 行を超えるファイルは Write ツールで作成すること。**

### このセッション自身がプロジェクトディレクトリをロックする

Bash ツールは毎回 cwd をプロジェクトディレクトリに戻すため、**自分自身のプロジェクトフォルダは移動できない**（`Device or resource busy`）。移動が必要ならセッションを閉じてから外部で行う。

---

## コマンド

| コマンド | 内容 |
|---|---|
| `npm run dev` | HMR 付き開発サーバー |
| `npm run build` | 型チェック + 本番ビルド |
| `npm run typecheck` | 型チェックのみ |
| `npm run test` | ユニットテスト |
| `npm run lint` | ESLint |
| `npm run format` | Prettier |

### ビルド後は `dist/` の配線を必ず検証する

ビルド成功は「正しく動く」を意味しない。**service worker ローダーと content script ローダーが別々の正しいチャンクを指しているか**を毎回確認すること。

```bash
cat dist/service-worker-loader.js
grep -o 'getURL("[^"]*")' dist/assets/*loader*.js
```

---

## git

ローカルの git は初期化済み（ブランチ `feat/data-fetch-layer`）。**GitHub 上のリポジトリはまだ未作成で remote も未設定。**

個人用アカウントで運用する。`~/.gitconfig` の `includeIf` により、`C:/Users/Owner/Documents/PersonalProject/` 配下では `kawagoe6884` が自動適用される。remote は SSH エイリアス経由で設定すること:

```
git@github-personal:kawagoe6884/<repo>.git
```

`github.com` 直指定や HTTPS は**仕事用の鍵に紐づくため使わない**。
